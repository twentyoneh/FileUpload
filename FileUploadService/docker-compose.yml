services:
  minio:
    image: minio/minio:latest
    container_name: fileupload-minio
    ports:
      - "9000:9000"   # S3 API — сюда будет стучаться твой код
      - "9001:9001"   # веб-консоль для просмотра бакетов глазами
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin123
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"

volumes:
  minio_data: